apiVersion: route.openshift.io/v1
kind: Route
spec:
  host: {{NICO_SITE_SITE_HOST_NAME_ENUS}}
  port:
    targetPort: http
  tls:
    insecureEdgeTerminationPolicy: Redirect
    termination: passthrough
  to:
    kind: Service
    name: nico-site
    weight: 100
  wildcardPolicy: None

